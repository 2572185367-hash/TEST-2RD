version: '3.8'
services:
  paper-search:
    build: .
    container_name: paper-keyword-search
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001
